{
    "version": "0.2.0",
    "configurations": [

        {
            "name": "Marc2Finc",
            "type": "debugpy",
            "request": "launch",
            "program": "${workspaceFolder}/marc2finc.py",
            "console": "integratedTerminal",
            "args": [
                "--source", "${workspaceFolder}/samples/output.mrc",
                "--target", "${workspaceFolder}/samples/test.json",
                "--schema", "${workspaceFolder}/schema/finc.yaml"
            ]
        }
    ]
}